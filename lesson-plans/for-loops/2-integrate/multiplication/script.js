import { readNumber, display } from '../../../../lib/dom-io.js';

document.getElementById("do-math").addEventListener('click', () => {
  debugger;

  // read user values
  let readNumber = 2;
  // use a for loop to multiply the two numbers
let sum = a;
for (let i = 0; i < (readNumber - 1); i++) {
  sum = sum * a;
}
console.log("sum: " + sum);
  // display the product
  display('sum', output);
});
